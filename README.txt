To install the 'notebook' command, run install.sh
To uninstall, run uninstall.sh


# Prepare directory
Navigate to the project directory and run 'notebook -i' to initialize the directory and copy the dependencies.


# Parse a source file
Navigate to the project directory and run 'notebook -p source.nb'.
